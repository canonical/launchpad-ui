// This file resides in `test` so that it is picked up by TS without needing to override `include` in the generated `.svelte-kit/tsconfig.json`.

import "../src/app.css";
import "@canonical/launchpad-design-tokens/dist/css/color/light.css"; // Run tests with a light theme
